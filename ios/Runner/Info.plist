<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>NSCameraUsageDescription</key>
	<string>Used only to scan the QR code on a venue sign for your Astronomy Passport. Camera images are processed on your device and never stored or sent anywhere.</string>
	<key>NSLocationWhenInUseUsageDescription</key>
	<string>Shows where you are on the campus map and points the compass toward venues at night. Your location is sent to Google only when you choose walking directions.</string>
	<!-- ITMS-90683. The app NEVER requests Always authorisation: geolocator's
	     PermissionHandler.m takes the requestWhenInUseAuthorization branch
	     whenever NSLocationWhenInUseUsageDescription is present, and the
	     requestAlwaysAuthorization branch above is unreachable while it is. But
	     that call is compiled into the statically-linked plugin, so Apple's scan
	     attributes it to Runner and requires this string. It is written for the
	     reviewer, and must stay truthful: no background modes, no
	     allowsBackgroundLocationUpdates, no Always prompt. -->
	<key>NSLocationAlwaysAndWhenInUseUsageDescription</key>
	<string>Shows where you are on the campus map and points the compass toward venues at night. The app only uses your location while it is open — it never uses background location. Your location is sent to Google only when you choose walking directions.</string>
	<key>NSMotionUsageDescription</key>
	<string>Used to sense which way you're facing so the map can point you toward the venues. This never leaves your device.</string>
	<!-- M4: Google Maps SDK key, substituted from the MAPS_API_KEY build setting
	     (ios/Flutter/Secrets.xcconfig, git-ignored). Resolves to "" when the secret
	     is absent, and AppDelegate then skips GMSServices.provideAPIKey → ships dark. -->
	<key>GMSApiKey</key>
	<string>$(MAPS_API_KEY)</string>
	<!-- Export compliance. An AUDITED conclusion, not a default: this key covers
	     the app AND its linked third-party libraries. Every networked dependency
	     was checked and each uses OS-provided TLS rather than bundling its own
	     cryptography — see docs/release/export-compliance.md for the working.
	     HTTPS via platform TLS is exempt, so this is false. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<key>CADisableMinimumFrameDurationOnPhone</key>
	<true/>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>Astronomy Open Night 2026</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>Astronomy Open Night 2026</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(FLUTTER_BUILD_NAME)</string>
	<key>CFBundleSignature</key>
	<string>????</string>
	<key>CFBundleVersion</key>
	<string>$(FLUTTER_BUILD_NUMBER)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<false/>
		<key>UISceneConfigurations</key>
		<dict>
			<key>UIWindowSceneSessionRoleApplication</key>
			<array>
				<dict>
					<key>UISceneClassName</key>
					<string>UIWindowScene</string>
					<key>UISceneConfigurationName</key>
					<string>flutter</string>
					<key>UISceneDelegateClassName</key>
					<string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
					<key>UISceneStoryboardFile</key>
					<string>Main</string>
				</dict>
			</array>
		</dict>
	</dict>
	<key>UIApplicationSupportsIndirectInputEvents</key>
	<true/>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<!-- Portrait-only, matching the SystemChrome lock in lib/main.dart: the app
	     is used one-handed while walking in the dark, so a rotation mid-stride
	     is never intentional. Restricting the plist too avoids a landscape
	     launch-screen flash on a device that starts out rotated. -->
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
</dict>
</plist>
